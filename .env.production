# Production env - used by deployment platform (e.g., Vercel). Do NOT commit real secrets to the repo.
# Set these variables in your Vercel/Neon project dashboard or CI secrets instead of committing.

DATABASE_URL="postgresql://<USER>:<PASSWORD>@<HOST>:5432/<DATABASE>?sslmode=require&pgbouncer=true"
DIRECT_URL="postgresql://<USER>:<PASSWORD>@<HOST>:5432/<DATABASE>?sslmode=require"
SCRAPE_API_KEY="changeme"
NEXT_PUBLIC_API_BASE=""
